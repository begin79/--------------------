# Полный анализ пользовательских и админских сценариев

## Дата анализа
09.12.2025 (обновлено после улучшений UX)

---

## Часть 1: Пользовательские сценарии

### Точка входа
**`/start`** — главное меню бота

---

### 1. Сценарий: Новый пользователь (первый запуск)

#### Путь пользователя:
1. **Пользователь** → `/start` → **Результат**: Приветствие + выбор режима
   - Кнопки: `🎓 Студент`, `🧑‍🏫 Преподаватель`

2. **Пользователь** → Выбирает `🎓 Студент` → **Результат**: Сообщение "Режим установлен: студента"
   - Кнопка: `⬅️ В главное меню`
   - **Состояние**: `CTX_MODE = "student"`, `CTX_AWAITING_DEFAULT_QUERY = None`

3. **Пользователь** → Вводит название группы (например, "ИС1-231") → **Результат**:
   - Если найдено 1 вариант → Показывается расписание с breadcrumbs
   - Если найдено несколько → Показывается список вариантов (ReplyKeyboard)
   - Если не найдено → Сообщение об ошибке + кнопка `🏠 В начало`

4. **Пользователь** → Выбирает вариант из списка → **Результат**: Показывается расписание с пагинацией
   - **Breadcrumbs**: `🎓 Расписание → Группы`
   - Кнопки: `⬅️ Назад` (если не первая страница), `🔄 Обновить`, `Вперед ➡️` (если не последняя)
   - Кнопка: `📤 Экспорт`
   - **НОВОЕ**: Кнопка `⭐ Установить по умолчанию` (если не установлена)
   - **НОВОЕ**: Кнопка `⚙️ Настройки`
   - Кнопка: `🏠 В начало`

5. **Пользователь** → Нажимает `⭐ Установить по умолчанию` → **Результат**:
   - Toast-уведомление: "✅ Группа 'ИС1-231' установлена по умолчанию!"
   - Расписание обновляется (кнопка "Установить по умолчанию" скрывается)
   - Уведомления автоматически включаются

6. **Пользователь** → После установки группы по умолчанию → **Результат**:
   - **НОВОЕ**: Показываются настройки (вместо главного меню)
   - Пользователь может сразу настроить время уведомлений

#### Улучшения:
- ✅ Добавлена кнопка "Установить по умолчанию" в расписание
- ✅ После установки группы показываются настройки
- ✅ Добавлены breadcrumbs для навигации
- ✅ Улучшена обработка ошибок с кнопкой "В начало"

---

### 2. Сценарий: Существующий пользователь (с установленной группой)

#### Путь пользователя:
1. **Пользователь** → `/start` → **Результат**: Главное меню с быстрым доступом
   - Кнопка: `📋 Показать расписание (ИС1-231...)` (если установлена группа)
   - Кнопки: `🎓 Студент`, `🧑‍🏫 Преподаватель`, `⚙️ Настройки`, `ℹ️ Помощь`

2. **Пользователь** → Нажимает `📋 Показать расписание` → **Результат**: Показывается расписание по умолчанию
   - Кнопки навигации, экспорт, настройки, "В начало"

3. **Пользователь** → Вводит другую группу → **Результат**: Показывается расписание для этой группы
   - Кнопка "Установить по умолчанию" доступна, если группа отличается от текущей

---

### 3. Сценарий: Настройки

#### Путь пользователя:
1. **Пользователь** → `/start` → `⚙️ Настройки` → **Результат**: Меню настроек
   - **Breadcrumbs**: `⚙️ Настройки`
   - Текущая группа/преподаватель
   - Время уведомлений
   - Кнопки:
     - `Установить/изменить группу`
     - `Установить/изменить преподавателя`
     - `✅/❌ Ежедневные уведомления`
     - `⏰ Изменить время уведомлений`
     - `💬 Оставить отзыв`
     - `♻️ Сбросить настройки`
     - `⬅️ Назад`

2. **Пользователь** → `Установить/изменить группу` → **Результат**: Запрос ввода группы
   - Кнопки: `❌ Отмена`, `🏠 В начало`
   - **Состояние**: `CTX_MODE = "student"`, `CTX_AWAITING_DEFAULT_QUERY = True`

3. **Пользователь** → Вводит название группы → **Результат**:
   - Если найдено точно → Устанавливается по умолчанию, возврат в настройки
   - Если найдено несколько → Показывается список (ReplyKeyboard)
   - Если не найдено → Сообщение об ошибке + кнопки `⬅️ Назад`, `🏠 В начало`

4. **Пользователь** → `✅/❌ Ежедневные уведомления` → **Результат**:
   - Toast-уведомление (включено/выключено)
   - Меню настроек обновляется

5. **Пользователь** → `⏰ Изменить время уведомлений` → **Результат**: Меню выбора времени
   - Кнопки: `07:00`, `18:00`, `19:00`, `20:00`, `21:00`, `22:00`, `23:00`
   - Кнопка: `⬅️ Назад` (в настройки)
   - После выбора → Toast-уведомление + возврат в настройки

6. **Пользователь** → `💬 Оставить отзыв` → **Результат**:
   - Если можно оставить → Форма отзыва с кнопкой `⬅️ Отмена`
   - Если нельзя (лимит 24 часа) → Popup с временем ожидания `⏳ Повторите через чч:мм:сс`
   - После отправки отзыва → Сообщение об успехе

7. **Пользователь** → `♻️ Сбросить настройки` → **Результат**: Подтверждение
   - Кнопки: `✅ Да, сбросить`, `❌ Отмена`
   - После подтверждения → Настройки сброшены, возврат в меню настроек

#### Улучшения:
- ✅ Добавлены breadcrumbs
- ✅ Улучшена обработка ошибок
- ✅ Добавлена кнопка "В начало" во все состояния ожидания ввода

---

### 4. Сценарий: Просмотр расписания

#### Путь пользователя:
1. **Пользователь** → Открывает расписание (любым способом) → **Результат**: Расписание с пагинацией
   - **Breadcrumbs**: `🎓 Расписание → Группы` или `🧑‍🏫 Расписание → Преподаватели`
   - Кнопки: `⬅️ Назад` (предыдущая страница), `🔄 Обновить`, `Вперед ➡️` (следующая страница)
   - Кнопка: `📤 Экспорт`
   - **НОВОЕ**: Кнопка `⭐ Установить по умолчанию` (если не установлена)
   - **НОВОЕ**: Кнопка `⚙️ Настройки`
   - Кнопка: `🏠 В начало`

2. **Пользователь** → `🔄 Обновить` → **Результат**: Расписание обновляется (перезагрузка с API)

3. **Пользователь** → `⬅️ Назад` / `Вперед ➡️` → **Результат**: Переход на предыдущую/следующую страницу

4. **Пользователь** → `🏠 В начало` → **Результат**: Возврат в главное меню

#### Улучшения:
- ✅ Добавлена кнопка "Настройки" в расписание
- ✅ Добавлена кнопка "Установить по умолчанию"
- ✅ Добавлены breadcrumbs

---

### 5. Сценарий: Экспорт расписания

#### Путь пользователя:
1. **Пользователь** → В расписании нажимает `📤 Экспорт` → **Результат**: Меню экспорта
   - Для студентов:
     - `🖼 Неделя (картинка)`
     - `📄 Неделя (PDF)`
     - `📸 По дням (картинки)`
     - `📊 Семестр (Excel)`
   - Для преподавателей:
     - `🖼 Неделя (картинка)`
     - `📄 Неделя (PDF)`
     - `📊 Семестр (Excel)`
   - Кнопка: `⬅️ Назад к расписанию`

2. **Пользователь** → Выбирает формат экспорта → **Результат**:
   - Показывается прогресс-бар
   - После завершения → Отправляется файл/изображение
   - Расписание остается открытым

3. **Пользователь** → `⬅️ Назад к расписанию` → **Результат**: Возврат к расписанию (восстанавливается состояние)

---

### 6. Сценарий: Справка

#### Путь пользователя:
1. **Пользователь** → `/start` → `ℹ️ Помощь` → **Результат**: Справка по боту
   - **Breadcrumbs**: `ℹ️ Справка`
   - **НОВОЕ**: Кнопка `⚙️ Настройки`
   - Кнопка: `🏠 В начало`

2. **Пользователь** → `/help` → **Результат**: Та же справка

#### Улучшения:
- ✅ Добавлена кнопка "Настройки" в справку
- ✅ Добавлены breadcrumbs

---

### 7. Сценарий: Inline режим (поиск в любом чате)

#### Путь пользователя:
1. **Пользователь** → В любом чате вводит `@Vgltu25_bot ИС1-231` → **Результат**:
   - Inline-результаты с расписанием
   - Пользователь может выбрать результат

2. **Пользователь** → Выбирает результат → **Результат**: Открывается расписание в боте

---

### 8. Сценарий: Умный холодный старт (определение типа запроса)

#### Путь пользователя:
1. **Пользователь** → Вводит группу/преподавателя без выбора режима → **Результат**:
   - Если у пользователя нет установленной группы → Предлагается выбрать режим
   - Если есть установленная группа → Предлагается подтвердить режим

2. **Пользователь** → Подтверждает режим → **Результат**: Показывается расписание

3. **Пользователь** → Отказывается от подтверждения → **Результат**:
   - **НОВОЕ**: Кнопка `🔍 Ввести другой запрос`
   - Кнопка `❌ Нет, выбрать другой` → Возврат в начало

#### Улучшения:
- ✅ Добавлена кнопка "Ввести другой запрос" при отказе от подтверждения

---

## Часть 2: Админские сценарии

### Точка входа
**`/admin`** — админ-панель (только для администраторов)

---

### 1. Сценарий: Вход в админ-панель

#### Путь администратора:
1. **Админ** → `/admin` → **Результат**: Главное меню админ-панели
   - Проверка прав администратора
   - Статистика: всего пользователей, активных за 24ч, всего запросов
   - Статус бота: 🟢 Включен / 🔴 Выключен
   - Количество непрочитанных отзывов
   - Кнопки:
     - `📊 Статистика`
     - `🟢/🔴 Управление ботом`
     - `👥 Управление пользователями`
     - `💬 Отзывы (N)` (с количеством непрочитанных)
     - `💬 Массовая рассылка`
     - `🗑️ Очистить кеш`
     - `👨‍💼 Управление админами`
     - `🚪 Выйти из админ-панели`

2. **Админ** → `🚪 Выйти из админ-панели` → **Результат**:
   - Очистка всех флагов админ-панели
   - Возврат в главное меню пользователя (`/start`)

---

### 2. Сценарий: Статистика

#### Путь администратора:
1. **Админ** → `📊 Статистика` → **Результат**: Детальная статистика
   - Пользователи: всего, активных за 24ч, с уведомлениями
   - Активность: всего запросов
   - Топ-5 активных пользователей
   - Время обновления
   - Кнопки: `🔄 Обновить`, `⬅️ Назад`

2. **Админ** → `🔄 Обновить` → **Результат**: Статистика обновляется

---

### 3. Сценарий: Управление статусом бота

#### Путь администратора:
1. **Админ** → `🟢/🔴 Управление ботом` → **Результат**: Меню управления статусом
   - Текущий статус: 🟢 Включен / 🔴 Выключен
   - Сообщение при выключении
   - Кнопки:
     - `🔴 Выключить бота` / `🟢 Включить бота`
     - `✏️ Изменить сообщение`
     - `⬅️ Назад`

2. **Админ** → `🔴 Выключить бота` → **Результат**: Подтверждение
   - Показывается сообщение, которое увидят пользователи
   - Кнопки: `✅ Да, подтверждаю`, `❌ Отмена`

3. **Админ** → `✅ Да, подтверждаю` → **Результат**:
   - Бот выключен/включен
   - Пользователи видят сообщение о техническом обслуживании при попытке использовать бота

4. **Админ** → `✏️ Изменить сообщение` → **Результат**: Запрос ввода нового сообщения
   - Кнопка: `❌ Отмена`
   - **Состояние**: `awaiting_maintenance_msg = True`

5. **Админ** → Вводит новое сообщение → **Результат**:
   - Сообщение обновлено
   - Возврат в меню управления статусом

---

### 4. Сценарий: Управление пользователями

#### Путь администратора:
1. **Админ** → `👥 Управление пользователями` → **Результат**: Меню управления пользователями
   - Кнопки:
     - `📋 Список пользователей`
     - `🔍 Найти пользователя`
     - `⬅️ Назад`

2. **Админ** → `📋 Список пользователей` → **Результат**: Список пользователей с пагинацией
   - Формат: номер, username, ID, группа (если есть), последняя активность
   - Главный администратор скрыт из списка
   - Для каждого пользователя: кнопка с именем и кнопка `✉️` (написать сообщение)
   - Навигация: `⬅️ Назад`, `🔄 Обновить`, `➡️ Далее`
   - Кнопка: `⬅️ Меню`

3. **Админ** → Нажимает на пользователя → **Результат**: Детальная информация о пользователе
   - ID, Username, дата создания, последняя активность
   - Расписание по умолчанию, режим
   - Статус уведомлений, время уведомлений
   - Последний отзыв (если есть)
   - Последний поиск/запрос
   - Кнопки:
     - `🔄 Обновить`
     - `✉️ Написать сообщение`
     - `⬅️ К списку`
     - `✉️ Открыть чат` (ссылка на Telegram)

4. **Админ** → `✉️ Написать сообщение` → **Результат**: Запрос ввода сообщения
   - Кнопки: `❌ Отмена`, `⬅️ К профилю`
   - **Состояние**: `awaiting_direct_message = True`, `direct_message_target = user_id`

5. **Админ** → Вводит сообщение → **Результат**:
   - Сообщение отправлено пользователю
   - Пользователь видит сообщение с кнопками: `✉️ Ответить`, `✅ Спасибо`
   - Админ видит подтверждение отправки
   - Кнопки: `✉️ Написать ещё`, `⬅️ К профилю`, `⬅️ Меню`

6. **Админ** → `🔍 Найти пользователя` → **Результат**: Запрос поискового запроса
   - Можно искать по ID или username
   - Кнопка: `❌ Отмена`
   - **Состояние**: `awaiting_user_search = True`

7. **Админ** → Вводит ID или username → **Результат**:
   - Если найден → Показывается информация о пользователе
   - Если не найден → Сообщение об ошибке
   - Кнопки: `🔍 Новый поиск`, `⬅️ Назад`

---

### 5. Сценарий: Работа с отзывами

#### Путь администратора:
1. **Админ** → `💬 Отзывы (N)` → **Результат**: Меню отзывов
   - Статистика: всего отзывов, непрочитанных
   - Кнопки:
     - `📋 Список отзывов`
     - `⬅️ Назад`

2. **Админ** → `📋 Список отзывов` → **Результат**: Список отзывов с пагинацией
   - Формат: маркер прочитанности (🆕/✅), номер, дата, имя пользователя, превью отзыва
   - Сортировка: новые сначала
   - Навигация: `⬅️ Предыдущая`, `Следующая ➡️`
   - Кнопка: `⬅️ Назад`

3. **Админ** → Нажимает на отзыв → **Результат**: Детали отзыва
   - ID отзыва, информация о пользователе, дата, полный текст отзыва
   - Кнопки:
     - `💬 Ответить пользователю` (переход к отправке сообщения)
     - `⬅️ К списку отзывов`
     - `🏠 В меню`

---

### 6. Сценарий: Массовая рассылка

#### Путь администратора:
1. **Админ** → `💬 Массовая рассылка` → **Результат**: Запрос ввода сообщения
   - Предупреждение о времени рассылки
   - Кнопка: `❌ Отмена`
   - **Состояние**: `awaiting_broadcast = True`

2. **Админ** → Вводит сообщение → **Результат**:
   - **Защита**: проверка на пустое сообщение, команды (начинающиеся с `/`), слишком короткие сообщения
   - Если валидно → Подтверждение рассылки
   - Показывается количество получателей
   - Показывается текст сообщения
   - Кнопки: `✅ Да, отправить всем`, `❌ Отмена`
   - **Состояние**: `broadcast_message = text`, `awaiting_broadcast = False`

3. **Админ** → `✅ Да, отправить всем` → **Результат**:
   - **Дополнительная защита**: проверка валидности сообщения
   - Начинается рассылка
   - Показывается прогресс: "📤 Начинаю рассылку для N пользователей..."
   - После завершения → Результаты: успешно, ошибок, всего
   - Кнопка: `⬅️ Назад`

#### Защита от ошибок:
- ✅ Проверка на пустые сообщения
- ✅ Защита от команд (начинающихся с `/`)
- ✅ Защита от слишком коротких сообщений (< 3 символов)
- ✅ Дополнительная проверка перед отправкой
- ✅ Очистка флагов после отправки

---

### 7. Сценарий: Управление администраторами

#### Путь администратора:
1. **Админ** → `👨‍💼 Управление админами` → **Результат**: Список администраторов
   - Список всех администраторов (кроме главного)
   - Для каждого: username, ID, дата добавления
   - Главный администратор скрыт и не может быть удалён
   - Кнопки:
     - `➕ Добавить админа`
     - `➖ Удалить админа`
     - `⬅️ Назад`

2. **Админ** → `➕ Добавить админа` → **Результат**: Запрос ввода ID
   - Инструкция: как узнать ID (через @userinfobot или /start)
   - Кнопка: `❌ Отмена`
   - **Состояние**: `awaiting_admin_id = True`

3. **Админ** → Вводит ID → **Результат**:
   - Если валидно → Администратор добавлен
   - Показывается ID и username нового админа
   - Кнопка: `⬅️ Назад`

4. **Админ** → `➖ Удалить админа` → **Результат**: Запрос ввода ID
   - Кнопка: `❌ Отмена`
   - **Состояние**: `awaiting_remove_admin_id = True`

5. **Админ** → Вводит ID → **Результат**:
   - **Защита**: нельзя удалить самого себя
   - **Защита**: нельзя удалить главного администратора
   - Если валидно → Администратор удалён
   - Кнопка: `⬅️ Назад`

---

### 8. Сценарий: Очистка кеша

#### Путь администратора:
1. **Админ** → `🗑️ Очистить кеш` → **Результат**: Кеш очищен
   - Очищаются: кеш расписания, кеш списков, кеш фотографий преподавателей
   - Кнопка: `⬅️ Назад`

---

### 9. Сценарий: Диалог админ ↔ пользователь

#### Путь пользователя:
1. **Пользователь** → Получает сообщение от админа → **Результат**: Сообщение с кнопками
   - Текст: "📬 Вам сообщение от команды расписания"
   - Кнопки: `✉️ Ответить`, `✅ Спасибо`

2. **Пользователь** → Нажимает `✉️ Ответить` → **Результат**: Запрос ввода ответа
   - Кнопка: `❌ Отмена`
   - **Состояние**: `awaiting_reply_to_admin = True`

3. **Пользователь** → Вводит ответ → **Результат**:
   - Ответ отправлен администратору
   - Администратор получает уведомление с текстом ответа
   - Кнопки: `✉️ Ответить`, `✅ Закрыть`

4. **Пользователь** → Нажимает `✅ Спасибо` → **Результат**:
   - Уведомление закрыто
   - Администратор получает уведомление о закрытии

#### Путь администратора:
1. **Админ** → Получает ответ от пользователя → **Результат**: Уведомление с ответом
   - Показывается ID пользователя, username, текст ответа
   - Кнопки: `✉️ Ответить`, `✅ Закрыть`

2. **Админ** → Нажимает `✉️ Ответить` → **Результат**: Запрос ввода ответа
   - Переход к функции отправки сообщения пользователю

---

## Общие проблемы и рекомендации

### Пользовательские сценарии

#### ✅ Исправлено:
1. ✅ Добавлена кнопка "Установить по умолчанию" в расписание
2. ✅ Добавлена кнопка "Настройки" в расписание
3. ✅ Показываются настройки после установки группы для новых пользователей
4. ✅ Добавлена кнопка "Настройки" в справку
5. ✅ Улучшена обработка ошибок (всегда есть кнопка "В начало")
6. ✅ Улучшена обработка отказа от подтверждения режима
7. ✅ Унифицированы тексты кнопок
8. ✅ Добавлены breadcrumbs в заголовки

#### 🟡 Рекомендации для дальнейшего улучшения:
1. Добавить историю поиска для быстрого доступа к недавним запросам
2. Добавить избранные группы/преподаватели
3. Добавить уведомления об изменениях в расписании (push-уведомления)
4. Добавить возможность поделиться расписанием с другими пользователями

### Админские сценарии

#### ✅ Сильные стороны:
1. ✅ Хорошая защита от ошибок в рассылке
2. ✅ Удобная навигация с возвратами
3. ✅ Детальная статистика и информация о пользователях
4. ✅ Система диалогов админ ↔ пользователь

#### 🟡 Рекомендации для дальнейшего улучшения:
1. Добавить фильтры в списке пользователей (по активности, режиму, уведомлениям)
2. Добавить экспорт статистики в Excel/CSV
3. Добавить планировщик рассылок (отложенная отправка)
4. Добавить шаблоны сообщений для быстрой отправки
5. Добавить логирование действий администраторов
6. Добавить возможность массового изменения настроек пользователей

---

## Карта навигации (полная схема)

### Пользовательская часть:
```
/start (Главное меню)
├── 🎓 Студент
│   └── Ввод группы → Расписание
│       ├── ⬅️ Назад (страница)
│       ├── 🔄 Обновить
│       ├── Вперед ➡️ (страница)
│       ├── 📤 Экспорт → Меню экспорта
│       ├── ⭐ Установить по умолчанию [НОВОЕ]
│       ├── ⚙️ Настройки [НОВОЕ]
│       └── 🏠 В начало
├── 🧑‍🏫 Преподаватель
│   └── Ввод ФИО → Расписание (аналогично)
├── ⚙️ Настройки
│   ├── Установить/изменить группу → Ввод → Установка
│   ├── Установить/изменить преподавателя → Ввод → Установка
│   ├── ✅/❌ Ежедневные уведомления → Toast
│   ├── ⏰ Изменить время → Меню времени → Выбор → Настройки
│   ├── 💬 Оставить отзыв → Форма / Popup (если лимит)
│   ├── ♻️ Сбросить настройки → Подтверждение → Настройки
│   └── ⬅️ Назад → Главное меню
└── ℹ️ Помощь
    ├── ⚙️ Настройки [НОВОЕ]
    └── 🏠 В начало → Главное меню
```

### Админская часть:
```
/admin (Админ-панель)
├── 📊 Статистика
│   ├── 🔄 Обновить
│   └── ⬅️ Назад
├── 🟢/🔴 Управление ботом
│   ├── Выключить/Включить → Подтверждение → Статус изменён
│   ├── ✏️ Изменить сообщение → Ввод → Сообщение обновлено
│   └── ⬅️ Назад
├── 👥 Управление пользователями
│   ├── 📋 Список пользователей → Пагинация → Профиль пользователя
│   │   ├── ✉️ Написать сообщение → Ввод → Сообщение отправлено
│   │   └── ⬅️ К списку
│   ├── 🔍 Найти пользователя → Ввод → Результаты поиска
│   └── ⬅️ Назад
├── 💬 Отзывы (N)
│   ├── 📋 Список отзывов → Пагинация → Детали отзыва
│   │   ├── 💬 Ответить пользователю → Ввод → Сообщение отправлено
│   │   └── ⬅️ К списку отзывов
│   └── ⬅️ Назад
├── 💬 Массовая рассылка
│   ├── Ввод сообщения → Подтверждение → Рассылка выполнена
│   └── ❌ Отмена
├── 🗑️ Очистить кеш → Кеш очищен
├── 👨‍💼 Управление админами
│   ├── ➕ Добавить админа → Ввод ID → Админ добавлен
│   ├── ➖ Удалить админа → Ввод ID → Админ удалён
│   └── ⬅️ Назад
└── 🚪 Выйти из админ-панели → Главное меню
```

---

## Выводы

### Пользовательские сценарии: **9/10**
- ✅ Навигация логична и интуитивна
- ✅ Все критические проблемы исправлены
- ✅ Добавлены breadcrumbs для ориентации
- ✅ Улучшена обработка ошибок
- ⚠️ Можно добавить дополнительные функции (история, избранное)

### Админские сценарии: **9/10**
- ✅ Хорошая защита от ошибок
- ✅ Удобная навигация
- ✅ Детальная информация о пользователях
- ✅ Система диалогов работает корректно
- ⚠️ Можно добавить фильтры и экспорт статистики

### Общая оценка: **9/10**
Система навигации хорошо продумана, все критические проблемы решены. Остались только рекомендации для дальнейшего улучшения функциональности.

