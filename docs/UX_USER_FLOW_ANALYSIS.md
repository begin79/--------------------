# Анализ пользовательских сценариев (User Flow)

## Дата анализа
09.12.2025

## Точка входа
**`/start`** — главное меню бота

---

## 1. Сценарий: Новый пользователь (первый запуск)

### Путь пользователя:
1. **Пользователь** → `/start` → **Результат**: Приветствие + выбор режима
   - Кнопки: `🎓 Студент`, `🧑‍🏫 Преподаватель`

2. **Пользователь** → Выбирает `🎓 Студент` → **Результат**: Сообщение "Режим установлен: студента"
   - Кнопка: `⬅️ В главное меню`
   - **Состояние**: `CTX_MODE = "student"`, `CTX_AWAITING_DEFAULT_QUERY = None`

3. **Пользователь** → Вводит название группы (например, "ИС1-231") → **Результат**:
   - Если найдено 1 вариант → Показывается расписание
   - Если найдено несколько → Показывается список вариантов (ReplyKeyboard)
   - Если не найдено → Сообщение об ошибке

4. **Пользователь** → Выбирает вариант из списка → **Результат**: Показывается расписание с пагинацией
   - Кнопки: `⬅️ Назад` (если не первая страница), `🔄 Обновить`, `Вперед ➡️` (если не последняя)
   - Кнопка: `📤 Экспорт`
   - Кнопка: `🏠 В начало`

### Проблемы:
- ❌ **Нет кнопки "Установить по умолчанию"** в расписании для новых пользователей
- ❌ После выбора режима пользователь должен вручную идти в настройки, чтобы установить группу по умолчанию
- ⚠️ Если пользователь вводит группу без выбора режима, бот предлагает выбрать режим, но это может быть неочевидно

---

## 2. Сценарий: Существующий пользователь (с установленной группой)

### Путь пользователя:
1. **Пользователь** → `/start` → **Результат**: Главное меню с быстрым доступом
   - Кнопка: `📋 Показать расписание (ИС1-231...)` (если установлена группа)
   - Кнопки: `🎓 Студента`, `🧑‍🏫 Преподавателя`, `⚙️ Настройки`, `ℹ️ Помощь`

2. **Пользователь** → Нажимает `📋 Показать расписание` → **Результат**: Показывается расписание по умолчанию
   - Кнопки навигации, экспорт, "В начало"

3. **Пользователь** → Вводит другую группу → **Результат**: Показывается расписание для этой группы
   - **Проблема**: Не сохраняется как новое по умолчанию автоматически

---

## 3. Сценарий: Настройки

### Путь пользователя:
1. **Пользователь** → `/start` → `⚙️ Настройки` → **Результат**: Меню настроек
   - Текущая группа/преподаватель
   - Время уведомлений
   - Кнопки:
     - `Установить/изменить группу`
     - `Установить/изменить преподавателя`
     - `✅/❌ Ежедневные уведомления`
     - `⏰ Изменить время уведомлений`
     - `💬 Оставить отзыв`
     - `♻️ Сбросить настройки`
     - `⬅️ Назад`

2. **Пользователь** → `Установить/изменить группу` → **Результат**: Запрос ввода группы
   - Кнопка: `Отмена`
   - **Состояние**: `CTX_MODE = "student"`, `CTX_AWAITING_DEFAULT_QUERY = True`

3. **Пользователь** → Вводит название группы → **Результат**:
   - Если найдено точно → Устанавливается по умолчанию, возврат в настройки
   - Если найдено несколько → Показывается список (ReplyKeyboard)
   - Если не найдено → Сообщение об ошибке + кнопка `⬅️ Назад` (в настройки)

4. **Пользователь** → `✅/❌ Ежедневные уведомления` → **Результат**:
   - Toast-уведомление (включено/выключено)
   - Меню настроек обновляется

5. **Пользователь** → `⏰ Изменить время уведомлений` → **Результат**: Меню выбора времени
   - Кнопки: `07:00`, `18:00`, `19:00`, `20:00`, `21:00`, `22:00`, `23:00`
   - Кнопка: `⬅️ Назад` (в настройки)
   - После выбора → Toast-уведомление + возврат в настройки

6. **Пользователь** → `💬 Оставить отзыв` → **Результат**:
   - Если можно оставить → Форма отзыва с кнопкой `⬅️ Отмена`
   - Если нельзя (лимит 24 часа) → Popup с временем ожидания `⏳ Повторите через чч:мм:сс`
   - После отправки отзыва → Сообщение об успехе

7. **Пользователь** → `♻️ Сбросить настройки` → **Результат**: Подтверждение
   - Кнопки: `✅ Да, сбросить`, `❌ Отмена`
   - После подтверждения → Настройки сброшены, возврат в меню настроек

### Проблемы:
- ✅ Навигация в настройках логична, есть возвраты
- ⚠️ После установки группы по умолчанию для нового пользователя показывается главное меню, а не настройки (может быть неочевидно)

---

## 4. Сценарий: Просмотр расписания

### Путь пользователя:
1. **Пользователь** → Открывает расписание (любым способом) → **Результат**: Расписание с пагинацией
   - Кнопки: `⬅️ Назад` (предыдущая страница), `🔄 Обновить`, `Вперед ➡️` (следующая страница)
   - Кнопка: `📤 Экспорт`
   - Кнопка: `🏠 В начало`

2. **Пользователь** → `🔄 Обновить` → **Результат**: Расписание обновляется (перезагрузка с API)

3. **Пользователь** → `⬅️ Назад` / `Вперед ➡️` → **Результат**: Переход на предыдущую/следующую страницу

4. **Пользователь** → `🏠 В начало` → **Результат**: Возврат в главное меню

### Проблемы:
- ✅ Навигация по страницам работает корректно
- ⚠️ Нет кнопки "Назад к настройкам" или "Назад к поиску" — только "В начало"

---

## 5. Сценарий: Экспорт расписания

### Путь пользователя:
1. **Пользователь** → В расписании нажимает `📤 Экспорт` → **Результат**: Меню экспорта
   - Для студентов:
     - `🖼 Неделя (картинка)`
     - `📄 Неделя (PDF)`
     - `📸 По дням (картинки)`
     - `📊 Семестр (Excel)`
   - Для преподавателей:
     - `🖼 Неделя (картинка)`
     - `📄 Неделя (PDF)`
     - `📊 Семестр (Excel)`
   - Кнопка: `⬅️ Назад к расписанию`

2. **Пользователь** → Выбирает формат экспорта → **Результат**:
   - Показывается прогресс-бар
   - После завершения → Отправляется файл/изображение
   - Расписание остается открытым

3. **Пользователь** → `⬅️ Назад к расписанию` → **Результат**: Возврат к расписанию (восстанавливается состояние)

### Проблемы:
- ✅ Навигация работает корректно, есть возврат к расписанию
- ⚠️ После экспорта нет явной кнопки "Вернуться к расписанию" в сообщении с файлом

---

## 6. Сценарий: Справка

### Путь пользователя:
1. **Пользователь** → `/start` → `ℹ️ Помощь` → **Результат**: Справка по боту
   - Кнопка: `🏠 В начало`

2. **Пользователь** → `/help` → **Результат**: Та же справка

### Проблемы:
- ✅ Есть возврат в начало
- ⚠️ Нет кнопки "Настройки" в справке для быстрого доступа

---

## 7. Сценарий: Inline режим (поиск в любом чате)

### Путь пользователя:
1. **Пользователь** → В любом чате вводит `@Vgltu25_bot ИС1-231` → **Результат**:
   - Inline-результаты с расписанием
   - Пользователь может выбрать результат

2. **Пользователь** → Выбирает результат → **Результат**: Открывается расписание в боте

### Проблемы:
- ✅ Работает корректно
- ⚠️ Нет явной информации о том, что это inline-режим

---

## 8. Сценарий: Умный холодный старт (определение типа запроса)

### Путь пользователя:
1. **Пользователь** → Вводит группу/преподавателя без выбора режима → **Результат**:
   - Если у пользователя нет установленной группы → Предлагается выбрать режим
   - Если есть установленная группа → Предлагается подтвердить режим

2. **Пользователь** → Подтверждает режим → **Результат**: Показывается расписание

### Проблемы:
- ⚠️ Может быть неочевидно, что нужно подтвердить режим
- ⚠️ Если пользователь отказывается от подтверждения, возврат только в начало, а не к поиску

---

## Общие проблемы навигации

### 🔴 Критические проблемы:

1. **Нет кнопки "Установить по умолчанию" в расписании**
   - Пользователь должен вручную идти в настройки
   - **Рекомендация**: Добавить кнопку "⭐ Установить по умолчанию" в расписание

2. **Тупики в навигации**
   - После просмотра расписания нет прямого возврата к настройкам
   - После экспорта нет явной кнопки возврата к расписанию в сообщении с файлом
   - **Рекомендация**: Добавить кнопку "⚙️ Настройки" в расписание

3. **Неочевидный возврат после установки группы по умолчанию**
   - Для новых пользователей показывается главное меню вместо настроек
   - **Рекомендация**: Показывать настройки после установки группы

### 🟡 Средние проблемы:

4. **Отсутствие хлебных крошек (breadcrumbs)**
   - Пользователь не всегда понимает, где он находится
   - **Рекомендация**: Добавить индикатор текущего раздела в заголовках

5. **Нет быстрого доступа к настройкам из справки**
   - **Рекомендация**: Добавить кнопку "⚙️ Настройки" в справку

6. **Неочевидная обработка ошибок**
   - При ошибках поиска пользователь остается в неопределенном состоянии
   - **Рекомендация**: Всегда показывать кнопку "🏠 В начало" при ошибках

### 🟢 Мелкие улучшения:

7. **Дублирование кнопок**
   - В главном меню есть и "Студент", и "Студента" (разные тексты)
   - **Рекомендация**: Унифицировать тексты кнопок

8. **Нет кнопки "Отмена" в некоторых состояниях ожидания ввода**
   - **Рекомендация**: Всегда показывать кнопку "Отмена" при ожидании ввода

---

## Рекомендации по улучшению навигации

### Приоритет 1 (Критично):
1. ✅ Добавить кнопку "⭐ Установить по умолчанию" в расписание
2. ✅ Добавить кнопку "⚙️ Настройки" в расписание
3. ✅ Показывать настройки после установки группы по умолчанию (для новых пользователей)

### Приоритет 2 (Важно):
4. ✅ Добавить кнопку "⚙️ Настройки" в справку
5. ✅ Всегда показывать кнопку "🏠 В начало" при ошибках
6. ✅ Улучшить обработку отказа от подтверждения режима (возврат к поиску, а не в начало)

### Приоритет 3 (Желательно):
7. ✅ Унифицировать тексты кнопок
8. ✅ Добавить индикатор текущего раздела (breadcrumbs)
9. ✅ Добавить кнопку "Отмена" во все состояния ожидания ввода

---

## Карта навигации (схема переходов)

```
/start (Главное меню)
├── 🎓 Студент
│   └── Ввод группы → Расписание
│       ├── ⬅️ Назад (страница)
│       ├── 🔄 Обновить
│       ├── Вперед ➡️ (страница)
│       ├── 📤 Экспорт → Меню экспорта
│       │   ├── 🖼 Неделя (картинка)
│       │   ├── 📄 Неделя (PDF)
│       │   ├── 📸 По дням (картинки) [только студенты]
│       │   ├── 📊 Семестр (Excel)
│       │   └── ⬅️ Назад к расписанию
│       └── 🏠 В начало
├── 🧑‍🏫 Преподаватель
│   └── Ввод ФИО → Расписание (аналогично)
├── ⚙️ Настройки
│   ├── Установить/изменить группу → Ввод → Установка
│   ├── Установить/изменить преподавателя → Ввод → Установка
│   ├── ✅/❌ Ежедневные уведомления → Toast
│   ├── ⏰ Изменить время → Меню времени → Выбор → Настройки
│   ├── 💬 Оставить отзыв → Форма / Popup (если лимит)
│   ├── ♻️ Сбросить настройки → Подтверждение → Настройки
│   └── ⬅️ Назад → Главное меню
└── ℹ️ Помощь
    └── 🏠 В начало → Главное меню
```

---

## Выводы

### Сильные стороны:
- ✅ Логичная структура главного меню
- ✅ Корректная работа экспорта с возвратом к расписанию
- ✅ Хорошая обработка состояний ожидания ввода
- ✅ Наличие кнопок возврата в большинстве мест

### Слабые стороны:
- ❌ Отсутствие быстрого доступа к настройкам из расписания
- ❌ Нет возможности установить группу по умолчанию прямо из расписания
- ❌ Неочевидные возвраты после некоторых действий
- ❌ Отсутствие индикации текущего раздела

### Общая оценка навигации: **7/10**
- Навигация в целом логична, но есть несколько критических мест, где пользователь может потеряться или не найти нужную функцию.

