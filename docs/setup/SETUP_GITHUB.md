# Инструкция по подключению к GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [GitHub](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку "+" в правом верхнем углу и выберите "New repository"
3. Заполните:
   - **Repository name**: `vgltu-schedule-bot` (или другое имя)
   - **Description**: "Telegram Bot для расписания ВГЛТУ"
   - **Visibility**: Private (рекомендуется, так как содержит токен) или Public
   - **НЕ** ставьте галочки на "Initialize this repository with a README" (у нас уже есть файлы)
4. Нажмите "Create repository"

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните следующие команды:

```bash
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Создайте первый коммит
git commit -m "Initial commit: Telegram bot for VGLTU schedule"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME и REPO_NAME на ваши)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

## Шаг 3: Настройте секреты (если используете GitHub Actions)

Если вы планируете использовать GitHub Actions для автоматического развертывания:

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте секрет `BOT_TOKEN` с вашим токеном бота

## Шаг 4: Настройте Issues и Projects

1. Убедитесь, что Issues включены: Settings → General → Features → Issues
2. Создайте шаблоны для Issues (уже созданы в `.github/ISSUE_TEMPLATE/`)
3. При необходимости создайте Project board для отслеживания задач

## Шаг 5: Защита главной ветки (опционально)

Рекомендуется настроить защиту главной ветки:

1. Settings → Branches → Add rule
2. Branch name pattern: `main`
3. Включите:
   - Require pull request reviews before merging
   - Require status checks to pass before merging

## Полезные команды Git

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить изменения на GitHub
git push

# Получить изменения с GitHub
git pull

# Создать новую ветку
git checkout -b feature/new-feature

# Переключиться на ветку
git checkout main

# Посмотреть историю коммитов
git log
```

## Важные замечания

⚠️ **ВАЖНО**: Файл `app/config.py` с токеном бота НЕ должен попасть в репозиторий!
Он уже добавлен в `.gitignore`, но убедитесь, что он не был случайно закоммичен:

```bash
# Проверьте, что config.py не отслеживается
git ls-files | grep config.py

# Если он есть в списке, удалите его из индекса:
git rm --cached app/config.py
git commit -m "Remove config.py from repository"
```

## Создание Issues

После подключения к GitHub вы можете создавать Issues для:
- 🐛 Сообщений об ошибках
- 💡 Предложений новых функций
- 📝 Вопросов и обсуждений
- 🔧 Запросов на улучшение

Используйте шаблоны из `.github/ISSUE_TEMPLATE/` для структурированных отчетов.

