# Инструкция по настройке бота на Amvera

## ✅ Что уже сделано

1. **Удалены чувствительные данные из `app/config.py`:**
   - Токен бота удален (теперь используется переменная окружения)
   - ADMIN_ID удален (теперь используется переменная окружения)

## 🔧 Переменные окружения для Amvera

В панели управления Amvera перейдите в раздел **"Переменные окружения"** и добавьте следующие переменные:

### Обязательные переменные:

```bash
BOT_TOKEN=ваш_токен_бота_от_BotFather
```

**Где взять токен:**
1. Откройте Telegram
2. Найдите бота [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/mybots`
4. Выберите вашего бота
5. Нажмите "API Token"
6. Скопируйте токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Опциональные переменные:

```bash
# ID администратора (ваш Telegram ID)
# Можно узнать у бота @userinfobot
ADMIN_ID=ваш_telegram_id

# Имя пользователя бота (по умолчанию: vgltusbot)
BOT_USERNAME=ваш_username_бота

# Директория для данных (по умолчанию: /data для Amvera)
DATA_DIR=/data
```

## 📝 Как узнать свой Telegram ID

1. Откройте Telegram
2. Найдите бота [@userinfobot](https://t.me/userinfobot)
3. Отправьте команду `/start`
4. Бот покажет ваш ID (число, например: `1003795435`)

## 🚀 Проверка после деплоя

После установки переменных окружения и деплоя:

1. Проверьте логи в панели Amvera
2. Откройте вашего бота в Telegram
3. Отправьте команду `/start`
4. Если бот отвечает - всё работает! ✅

## ⚠️ Важные замечания

- **НЕ добавляйте** файл `app/config.py` с реальными значениями в Git репозиторий
- Все чувствительные данные должны быть только в переменных окружения Amvera
- Файл `app/config.py.example` можно коммитить - это шаблон без реальных данных

## 🔒 Безопасность

- Токен бота - это секретная информация, не делитесь им ни с кем
- Не публикуйте токен в открытых репозиториях
- Если токен был скомпрометирован, немедленно получите новый у @BotFather

