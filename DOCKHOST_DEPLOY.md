# Размещение бота на DockHost

[DockHost](https://my.dockhost.ru/) - это российский хостинг-провайдер, который предоставляет Docker-контейнеры для размещения приложений.

## ✅ Преимущества DockHost

- ✅ **Поддержка Python** - можно использовать любой Python-код
- ✅ **Docker-контейнеры** - полный контроль над окружением
- ✅ **SQLite работает** - можно использовать локальную базу данных
- ✅ **Постоянное хранилище** - данные сохраняются между перезапусками
- ✅ **Российский хостинг** - быстрый доступ из России
- ✅ **Простой деплой** - через Git или Dockerfile

## 📋 Подготовка к деплою

### Шаг 1: Создать аккаунт на DockHost

1. Перейдите на https://my.dockhost.ru/
2. Зарегистрируйте аккаунт
3. Выберите тарифный план

### Шаг 2: Подготовить проект

Проект уже готов! В репозитории есть:
- ✅ `Dockerfile` - для сборки образа
- ✅ `.dockerignore` - исключает ненужные файлы
- ✅ `requirements.txt` - зависимости Python

### Шаг 3: Настроить переменные окружения

Вам нужно будет установить следующие переменные окружения в панели DockHost:

- `BOT_TOKEN` - токен вашего Telegram-бота (получить у @BotFather)
- `ADMIN_ID` (опционально) - ID администратора бота

## 🚀 Деплой на DockHost

### Вариант 1: Через Git (рекомендуется)

1. **Подключите репозиторий Git:**
   - В панели DockHost перейдите в раздел "Проекты"
   - Нажмите "Создать проект"
   - Выберите "Из Git репозитория"
   - Укажите URL вашего репозитория (GitHub, GitLab и т.д.)

2. **Настройте сборку:**
   - DockHost автоматически обнаружит `Dockerfile`
   - Укажите команду запуска: `python new_VGLTU_bot.py`
   - Или оставьте по умолчанию (Dockerfile уже содержит CMD)

3. **Установите переменные окружения:**
   - В настройках проекта добавьте:
     - `BOT_TOKEN=ваш_токен_бота`
     - `ADMIN_ID=ваш_telegram_id` (опционально)

4. **Запустите проект:**
   - Нажмите "Запустить" или "Деплой"
   - DockHost соберет образ и запустит контейнер

### Вариант 2: Через Dockerfile напрямую

1. **Создайте проект:**
   - В панели DockHost выберите "Создать проект"
   - Выберите "Из Dockerfile"
   - Загрузите или вставьте содержимое `Dockerfile`

2. **Настройте переменные окружения** (как в варианте 1)

3. **Запустите проект**

## 📁 Настройка постоянного хранилища

Для сохранения базы данных и файлов между перезапусками:

1. В настройках проекта найдите раздел "Volumes" или "Хранилище"
2. Добавьте volume:
   - **Путь в контейнере:** `/data`
   - **Тип:** Постоянное хранилище
3. Это сохранит:
   - `users.db` - база данных пользователей (в `/data/users.db`)
   - `bot_data.pickle` - данные бота (в `/data/bot_data.pickle`)
   - Другие файлы в директории `/data/`

**Важно:** Бот по умолчанию использует путь `/data/users.db` для базы данных. Убедитесь, что volume подключен к `/data`, а не к `/app/data`.

## 🔧 Настройка базы данных

### Автоматическое создание

Бот автоматически создаст базу данных при первом запуске в директории `/app/data/`.

### Миграция с Amvera

Если у вас уже есть база данных на Amvera:

1. **Скачайте базу данных:**
   - На Amvera: скачайте файл `users.db` из директории `/data/`

2. **Загрузите на DockHost:**
   - Через панель управления DockHost загрузите файл в volume `/data/`
   - Или используйте команду в контейнере:
     ```bash
     docker cp users.db container_name:/data/users.db
     ```

3. **Проверьте права доступа:**
   - Убедитесь, что файл доступен для записи

## 🔍 Проверка работы

После деплоя проверьте:

1. **Логи контейнера:**
   - В панели DockHost откройте раздел "Логи"
   - Должны появиться сообщения о запуске бота

2. **Тест бота:**
   - Откройте Telegram
   - Найдите вашего бота
   - Отправьте команду `/start`
   - Бот должен ответить

## ⚙️ Дополнительные настройки

### Автоперезапуск

DockHost обычно автоматически перезапускает контейнер при сбоях. Убедитесь, что в настройках проекта включен "Auto-restart".

### Мониторинг

- Используйте логи в панели DockHost для мониторинга
- Настройте уведомления о сбоях (если доступно)

### Обновление бота

1. **Через Git:**
   - Сделайте изменения в коде
   - Закоммитьте и запушьте в репозиторий
   - В панели DockHost нажмите "Пересобрать" или "Обновить"

2. **Вручную:**
   - Загрузите обновленные файлы
   - Перезапустите контейнер

## 🆚 Сравнение с Amvera

| Функция | DockHost | Amvera |
|---------|----------|--------|
| Поддержка Python | ✅ | ✅ |
| SQLite | ✅ | ✅ |
| Docker | ✅ | ✅ |
| Постоянное хранилище | ✅ | ✅ |
| Простота деплоя | ⚠️ Средняя | ✅ Высокая |
| Российский хостинг | ✅ | ✅ |
| Бесплатный тариф | ⚠️ Проверьте | ✅ Есть |

## ❓ Решение проблем

### Бот не запускается

1. Проверьте логи в панели DockHost
2. Убедитесь, что `BOT_TOKEN` установлен правильно
3. Проверьте, что все зависимости установлены

### База данных не сохраняется

1. Убедитесь, что volume настроен правильно (путь `/data`)
2. Проверьте права доступа к директории `/data`
3. Убедитесь, что путь к базе данных правильный (`/data/users.db`)
4. Проверьте логи - там должно быть сообщение "Используется файл базы данных: /data/users.db"

### Ошибки импорта

1. Проверьте, что все зависимости в `requirements.txt`
2. Убедитесь, что `PYTHONPATH=/app` установлен
3. Проверьте структуру проекта

## 📞 Поддержка

- Документация DockHost: https://my.dockhost.ru/docs (если доступно)
- Поддержка: через панель управления DockHost

## 🎯 Рекомендации

1. **Начните с тестового деплоя** - проверьте работу на тестовом боте
2. **Сделайте резервную копию базы данных** перед миграцией
3. **Мониторьте логи** в первые дни после деплоя
4. **Настройте автоперезапуск** для надежности

Удачи с деплоем! 🚀

