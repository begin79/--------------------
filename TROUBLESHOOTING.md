# Решение проблем с запуском бота

## ❌ Ошибка 409 Conflict

**Симптомы:**
```
HTTP Request: POST .../getUpdates "HTTP/1.1 409 Conflict"
⚠️ Обнаружен конфликт: уже запущен другой экземпляр бота
```

**Причина:**
Telegram API не позволяет запускать несколько экземпляров бота с одним токеном одновременно.

**Решение:**

### 1. Проверка запущенных процессов

**Windows (PowerShell):**
```powershell
Get-Process python* | Where-Object {$_.Path -like "*python*"}
```

**Остановка процесса:**
```powershell
Stop-Process -Id <PID> -Force
```

### 2. Проверка сервера

Если бот запущен на сервере (Amvera/другой хостинг):
- Зайдите в панель управления хостингом
- Остановите бота там
- Подождите 5-10 секунд
- Запустите локально

### 3. Использование скрипта проверки

```bash
python check_bot_status.py
```

(Требует установки psutil: `pip install psutil`)

### 4. Ручная проверка

1. Закройте все окна терминала
2. Проверьте диспетчер задач (Ctrl+Shift+Esc)
3. Найдите процессы Python
4. Закройте процессы, связанные с ботом

## ✅ Правильный запуск

```bash
# Убедитесь, что нет других экземпляров
python check_bot_status.py

# Запустите бота
python run.py
```

## 🔍 Дополнительная диагностика

### Проверка токена
Убедитесь, что токен правильный в `app/config.py`:
```python
TOKEN = os.getenv("BOT_TOKEN", "ваш_токен")
```

### Проверка базы данных
```python
python -c "from app.database import db; print(f'Пользователей: {len(db.get_all_users())}')"
```

### Логи
Все логи выводятся в консоль. Ищите:
- ✅ Успешный запуск
- ❌ Ошибки конфликта
- ⚠️ Предупреждения

## 📝 Частые проблемы

1. **Бот запущен на сервере и локально**
   → Остановите один из экземпляров

2. **Несколько окон терминала**
   → Закройте все, кроме одного

3. **Бот запущен как служба**
   → Остановите службу через services.msc

4. **Старый процесс не завершился**
   → Перезагрузите компьютер или завершите процесс вручную

